<b:with value="data:posts" var="posts">
  <b:loop index="i" values="data:posts" var="post">
    <article class="post">

      <header>

        {{> partial-breadcrumbs}}

        <h1 class="card-title">
          <b:eval
            expr='data:post.title.escaped != "" ? data:post.title.escaped : data:messages.noTitle'
          />
        </h1>
        <p class="post-meta">
          <data:post.date />
        </p>
      </header>

      <div class="content">
        <data:post.body />
      </div>

      <div class="post-related">
        <script type="text/javascript">
        const postCategories = [<b:if cond='data:post.labels'><b:loop values='data:post.labels' var='label'>&quot;<data:label.name/>&quot;<b:if cond='data:label.isLast != &quot;true&quot;'>,</b:if></b:loop></b:if>];
        /*<![CDATA[*/{{asset
            "dist/js/related_post.min.js"
          }}/*]]>*/</script>
      </div>

    </article>
  </b:loop>
</b:with>
